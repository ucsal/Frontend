// Configuração para habilitar/desabilitar modo mock
export const MOCK_CONFIG = {
  // Altere para false quando o backend estiver pronto
  ENABLE_MOCK: true,

  // Atraso artificial para simular rede (ms)
  DELAY: 500,
};

export default MOCK_CONFIG;
